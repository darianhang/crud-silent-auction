{
    "name": "gensler-silent-auction",
    "version": "1.0.0",
    "description": "",
    "main": "app.js",
    "scripts": {
      "build":"cd client && npm run build", 
      "install-client": "cd client && npm install",
      "heroku-postbuild": "cd client && npm install && npm run build",
      "start": "cd api && node server.js"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
      "express-handlebars": "^3.1.0"
    }
  }
